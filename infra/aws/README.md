# Legacy S3/CDN example

Use [the modular environment roots](../README.md) for the Cloud/Scale/Reliability upgrade. This old standalone root is retained as a state-migration reference, not a second production deployment path. Do not apply it and the new roots to the same bucket or distribution. Existing resources require reviewed imports/state moves; they are not moved automatically.
